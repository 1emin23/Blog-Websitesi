<%- include('../partials/header') %>

    <div class="page-container">
        <div class="content-wrapper">
            <h1 class="error-title">404 - Sayfa Bulunamadı</h1>
            <p class="error-message">Üzgünüz, aradığınız sayfa mevcut değil veya kaldırılmış olabilir.</p>
            <a href="/" class="action-button">Anasayfaya Dön</a>
        </div>
    </div>

    <style>
        .page-container {
            min-height: 100vh;
            padding: 2rem 1rem;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f0f2f5;
        }

        .content-wrapper {
            text-align: center;
            max-width: 600px;
            margin: 0 auto;
        }

        .error-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #1a202c;
            margin-bottom: 1rem;
        }

        .error-message {
            font-size: 1.125rem;
            color: #4b5563;
            margin-bottom: 2rem;
        }

        .action-button {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background-color: #4f46e5;
            color: #fff;
            text-decoration: none;
            border-radius: 6px;
            font-size: 1rem;
            font-weight: 500;
            transition: background-color 0.3s ease;
        }

        .action-button:hover {
            background-color: #4338ca;
        }

        @media (max-width: 768px) {
            .error-title {
                font-size: 2rem;
            }

            .error-message {
                font-size: 1rem;
            }

            .action-button {
                padding: 0.6rem 1.2rem;
                font-size: 0.875rem;
            }
        }
    </style>

    <%- include('../partials/footer') %>